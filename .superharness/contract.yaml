# Active contract for youtube-model-feeder
id: initial-setup
created: 2026-03-26
created_by: owner
status: draft

goal: "TBD — describe the current objective"

tasks:
- id: docker-hub-publish
  title: "Audit gitignore, write real Dockerfiles, and publish to Docker Hub"
  owner: claude-code
  status: todo
  description: >
    The project has real app code locally (services/, frontend/, migrations/, .venv312/)
    that is gitignored. Before Docker Hub publish:
    1. Audit .gitignore — understand what's intentionally excluded vs accidentally excluded.
       Check if services/, migrations/, frontend/ should be tracked. Never commit .env.
    2. Write real backend/Dockerfile using uv + pyproject.toml + actual dep tree.
    3. Write real frontend/Dockerfile (Next.js multi-stage build).
    4. Fix docker-compose.yml build contexts to match actual file structure.
    5. Build images, tag as celstnblacc/youtube-model-feeder-backend:0.1.x, push to Docker Hub.
  risks: >
    - .env must not be committed or baked into images
    - docker-compose.yml references ./backend/Dockerfile which doesn't exist yet
    - uv.lock has real deps but pyproject.toml is sparse — full dep audit needed first
  project_path: /Users/airm2max/DevOpsSec/youtube-model-feeder

decisions: []

failures: []
