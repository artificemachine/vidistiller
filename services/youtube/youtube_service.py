# YouTube Service Module
# Initialize YouTube Data API client with API key from settings
# Implement video metadata fetching (title, description, duration, etc.)
# Validate YouTube URLs and extract video IDs
# Handle pagination for playlists and video lists
# Implement error handling for invalid URLs, rate limiting, quota exceeded
# Cache video metadata to reduce API calls
# Support both direct video URLs and playlist URLs
# Extract thumbnail images for documentation
# Handle age-restricted and private video restrictions
# Implement retry logic with exponential backoff
# Log all API interactions for debugging
# Create data models for YouTube responses (VideoMetadata, PlaylistInfo, etc.)
# Use yt-dlp for video downloading and metadata extraction
# Handle cookies and authentication for restricted content
# Implement progress callbacks for download operations
