ignore:
  rules: []
  # The GHA-002 suppression (shipguard 0.4.3 false positive on SHA-pinned
  # `uses:` lines) was removed once the project moved to shipguard 0.5.2, where
  # the rule no longer fires. Verified: `shipguard scan .` with no exclusions
  # reports 0 GHA-002 findings.
