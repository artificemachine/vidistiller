# Makefile for Docker and application management
# Provides convenient commands for building, running, and maintaining containers
# Simplifies common development and deployment workflows

# Build all Docker images

# Start all services in detached mode

# Stop all running containers

# View logs from services

# Run migrations and initialization

# Execute commands inside containers

# Clean up containers, images, and volumes

# Run tests inside containers

# Format and lint code

# Health check all services
