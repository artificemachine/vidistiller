SCOPE: General audit (no focus invariant) across `artificemachine/vidistiller` — Python/FastAPI backend + Next.js/TypeScript frontend. Doctrine harvested from `CLAUDE.md`, `AGENTS.md`, `SOUL.md`, `README.md`, `CONTRIBUTING.md`, `SECURITY.md`, and 13 `docs/*.md` files. No prior bulletproof report exists — this is the baseline.

CLAIMS AUDITED

| Claim | Source | Verdict | Evidence |
|-------|--------|---------|----------|
| "start only Postgres + Redis for host-side tests" | `AGENTS.md:14` | VERIFIED | `docker-compose.test.yml` `services:` = exactly `['postgres', 'redis']` |
| `repo-guardrails` skill exists at `$HOME/.codex/skills/repo-guardrails` | `AGENTS.md:29` | VIOLATED | Path does not exist on this machine (`ls` fails); the two sibling skill references on the same lines (`githooks-inspector`, `security-pipeline`) both resolve correctly, so this is an isolated broken reference, not a stale convention |
| `CLAUDE.md` "protected... only modified when the user gives an explicit instruction that names `CLAUDE.md`" | `AGENTS.md:45-46` | VERIFIED (behaviorally, this session) | Confirmed empirically this session: an implicit edit was made, caught, and reverted before proceeding; the follow-up edit only proceeded after the user explicitly named `CLAUDE.md`. Process claim, not code-enforced — no automated guard exists (see REMEDIATION) |
| "Never push directly to main without human review" | `SOUL.md:9` | **VIOLATED** (the "human review" clause) | Branch protection has `required_status_checks` (backend-tests, frontend-tests, security, scan) + `enforce_admins: true`, which does block a raw `git push` of an unreviewed commit — but `required_pull_request_reviews` is absent entirely. Empirically: the last 20 merged PRs carry **0 reviews total** (`gh pr list --state merged --limit 20 --json reviews`). "Directly" is blocked; "human review" is not enforced and does not happen in practice. This is a known, accepted trade-off for a solo-maintained repo (documented in `docs/audits/2026-07-21-portfolio-ready.md`), not a surprise — but the doctrine's literal wording overclaims what's enforced |
| `.env`/credentials/secrets "never edited" | `SOUL.md:11` | UNCHECKABLE | Agent-behavior claim; `.gitignore` prevents tracking (`grep -c '^\.env$' .gitignore` = 2 matching entries) but that's a tracking backstop, not proof no edit ever happens. No probe can verify a negative behavioral claim |
| `llm.py` sends transcript+snapshots "to LLM (Ollama, OpenAI, or Anthropic)" | `README.md:216`; same undercount in `backend/app/services/llm.py:5` module docstring | **VIOLATED** | `backend/app/services/llm_providers.py` defines 4 provider classes: `OllamaProvider:34`, `OpenAIProvider:76`, `AnthropicProvider:112`, `VLLMProvider:148`. Both the README and the module's own docstring list only 3, omitting vLLM |
| "**Ollama** is needed for LLM ambiguity classification... only used for borderline transitions" (Presentation Mode Requirements) | `README.md:135` | **VIOLATED** | `backend/app/services/slide_detection.py:181-182,466-468`: the classifier is fully provider-agnostic — "runs on the same vLLM fleet / provider the rest of the app uses. The provider is injected by the caller." Nothing in the slide-detection code path requires or special-cases Ollama; the README implies an Ollama-specific dependency for a feature that actually inherits whatever provider the job owner configured |
| "migrations/... ensuring every environment (local, CI, production) has an identical database schema" | `README.md:220` | **VIOLATED** | `backend/app/main.py:108,117-119`: the app's own startup lifespan calls `Base.metadata.create_all()` plus three hand-written `ALTER TABLE` statements *independent of Alembic*, every time the process boots. Two schema-management paths exist; a fresh `docker compose up -d` builds schema via `create_all`+ALTER, not via `alembic upgrade head` (that only runs if invoked separately, per the documented quickstart step 3). This is the same dual-path finding independently flagged as a MED architecture issue in `docs/audits/2026-07-21-job-ready.md`'s Final Certification — bulletproof corroborates it here as a doc-drift violation, not just an architecture smell |
| `backend/app/core/config.py` reads env vars "with validation and sane development defaults, so `docker compose up -d` boots without any required configuration" | `README.md:229` | VERIFIED (as of `v1.12.5`) | This was **false** earlier this session — a blank `JWT_SECRET_KEY` (the exact scenario `docker compose up -d` produces from `.env.example`) was rejected outright until fixed in `backend/app/core/config.py`'s `validate_jwt_secret` (PR #129). Re-probed against current `main`: the blank-secret-treated-as-unset fix is present and covered by `tests/test_config.py::test_blank_jwt_secret_key_treated_as_unset_in_development`. Claim now true; flagged here so its history is on record — it was VIOLATED as recently as this session |
| No two doctrine files name contradictory sources of truth | cross-file scan | VERIFIED | 3 distinct "source of truth"/"canonical" statements found (`.superharness/state.db`, root `README.md` for project overview, `deploy/terraform/vidistiller.tf` for provisioning) — each names a different, non-overlapping entity. No contradiction |
| Every file/symbol named in `docs/README.md`'s index resolves on disk | `docs/README.md` (rewritten this session, PR #131/#133) | VERIFIED | All 8 linked files present; re-probed fresh this run, not reused from the prior audit's finding (which was about the *pre-fix* index) |

HONESTY SCORE: **6/10** completion/invariant claims true as stated (VERIFIED), 4/10 VIOLATED, 1 UNCHECKABLE (behavioral). Blunt summary: **the doctrine oversells provider coverage and schema-management purity, and one guardrail's wording ("human review") doesn't match this repo's actual, accepted practice.** None of the violations are security-relevant or user-facing; all are documentation precisely describing less than the code actually does or claiming enforcement that doesn't fully exist.

NOT AUDITED (out of focus)
- `docs/audits/*.md`, `docs/PLAN-*.md`, `docs/AUDIT-*.md` — excluded per the skill's own rule (dated snapshots by design, not living documentation)
- Full drift-class sweep of `frontend/` TypeScript comments/docstrings — this run focused on root+docs doctrine and the claims those directly implicated; a frontend-code-comment sweep would need a separate pass
- Mermaid/Excalidraw diagram parsing — the README's architecture diagram is plain ASCII art, not a Mermaid fence or `.excalidraw` file, so the diagram-specific probes in this skill's pipeline don't apply here

DRIFT-CLASS FINDINGS
- **Dead/orphaned reference**: `AGENTS.md:29`'s `repo-guardrails` skill path is dangling — either the skill was never installed on this machine, was renamed, or the doc was copied from a different environment. Low severity (Codex-only tooling reference, doesn't affect this repo's own code), but exactly the kind of thing that erodes trust in the rest of AGENTS.md's instructions once a reader hits one broken one.
- **Silent-success risk**: none found beyond the dual-schema-path finding above (already counted as a doc-drift VIOLATED claim, not double-counted here).
- **Unenforced invariant**: the `CLAUDE.md`-protection rule (`AGENTS.md:45-46`) has no code-level guard — it's pure convention, relying on every agent reading and honoring the doc. A pre-commit hook could mechanically enforce "block a diff touching CLAUDE.md unless a marker/flag is present," turning this from a trust-based rule into a guaranteed one.

REMEDIATION
- **Manifest** (proposed entities + booleans, not yet written — flag before creating, see below):
  - `llm_providers_documented`: README/module-docstring provider list == `grep -c '^class.*Provider' backend/app/services/llm_providers.py`
  - `schema_single_path`: either (a) remove `create_all`+ALTER from `main.py` lifespan in favor of `alembic upgrade head` at deploy, or (b) reword the README/doctrine claim to describe both paths honestly
  - `agents_md_skill_paths_resolve`: every `$HOME/.codex/skills/<name>` path named in `AGENTS.md` exists on the machine the doc ships with (or is scoped as "install these yourself" rather than asserted as present)
- **Guard** (not written — this is audit mode, no `--emit-guard` flag was requested): a plain Python test iterating `backend/app/services/llm_providers.py`'s `Provider` subclasses and asserting the same count appears in `README.md`'s provider-list line and `llm.py`'s module docstring would catch this specific drift on every future provider addition/removal.

PROGRESS (vs baseline)
- First run — no prior report to diff against. This file is now the baseline for the next `/bulletproof` invocation.
