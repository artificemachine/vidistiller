# Job-Ready Scorecard — vidistiller
**Date:** 2026-07-20 · Modes: full default run (morning), `--quick` validation (afternoon), full default re-run with updated skill (evening — Stage 4 reproduced, stages 5–8 condensed per Degradation) · Repo: github.com/artificemachine/vidistiller (PUBLIC)

| # | Stage | Verdict | Blockers |
|---|-------|---------|----------|
| 1 | First impression | FAIL | 2 HIGH (infra-topology leak, history PII) + 4 |
| 2 | Git history & releases | FAIL | 33 local + 78 remote deletable branches (PR-verified), version drift |
| 3 | README + docs | NEEDS WORK | 4 broken links, stale AWS claim, docs/ convention |
| 4 | Fresh clone + deps | **FAIL** | **quickstart broken from fresh clone — reproduced 2×**; 1 HIGH npm CVE |
| 5 | Hardening (condensed gauntlet) | PASS* | 0 CRIT/HIGH; 14 MED shipguard |
| 6 | Architecture | PASS* | condensed pass; full /arch-audit recommended |
| 7 | CI governance | FAIL | no branch protection; gitleaks scans only latest commit |
| 8 | Claims vs reality | FAIL | quickstart + AWS-terraform + version claims falsified |

\* condensed evidence-based pass (component skills), not the full /gauntlet + /arch-audit runs.

## Verdict: NOT READY

Hard gates triggered: quickstart fails from a fresh clone (Stage 4), 1 HIGH dependency CVE (form-data), unpatched history findings CI never sees (Stages 1/7).

## Top 5 fixes by interview impact

1. **Fix the dev-compose quickstart** — `./backend` bind mount shadows `/app/deps` (pip --target). Add an anonymous volume over `/app/deps` in docker-compose.yml (or drop --target in the Dockerfile). **Reproduced twice** (morning manual run + evening run via the new `scripts/fresh-clone.sh`, which auto-tore-down correctly). Then re-verify: fresh clone → `docker compose up -d` → all services healthy. This is the single most damaging finding: a reviewer who tries the repo hits a crash in 2 minutes.
2. **Scrub internal homelab topology from tracked files** — `deploy/` (terraform/ansible: node names, 10.255.x.x subnets, bridges, ProxyJump), `.env.example` fleet IPs, `.github/workflows/deploy.yml:187`, `.superharness/` (8 files). Replace with placeholders (`10.0.0.10`, `your-host.example.com`) or move `deploy/` to a private repo. This matches the posture already applied to sentinel_networks/proxmox_clusters.
3. **Enable branch protection on main** — require test.yml + security.yml + gitleaks.yml status checks, disallow force-push. Five minutes in repo settings; the single cheapest credibility fix.
4. **Repair README surface** — fix 4 broken links (DESIGN_EXPORT_GUIDE.md, DESIGN_README.md, docs/DOCKER.md, docs/DEPLOYMENT.md), correct the terraform/AWS description, add a demo GIF/screenshot above the fold, drop the pink span styling, align the Python version story (3.12 everywhere or 3.14 everywhere).
5. **Version + dependency hygiene** — single-source the version (pyproject 1.10.6 / frontend 1.10.11 / tag v1.10.10 disagree), tag v1.10.11, run `npm audit fix` (form-data HIGH), switch CI to `npm ci`, delete the PR-verified merged branches (33 local + 78 remote — delete list reproducible via `gh pr list --state merged` head refs).

Also worth doing: extend gitleaks CI to periodic full-history scans; replace the real-looking example Fernet key in .env.example with a placeholder; add SECURITY.md + issue/PR templates; move DESIGN_SPEC.md/ROADMAP.md/TECH_STACK.md/VidDocs_UI_UX_Audit_Report.md into docs/; untrack `features_to_add/` (contains a .docx with a personal name).

## What this repo says about you (honest read)

A senior engineer walks away with a split impression. The engineering substance is real and above portfolio average: 411 backend + 224 frontend tests passing, e2e harness, SHA-pinned CI with an enforced severity gate, disciplined conventional-commit history with squash-merged PRs, 20+ documented releases, non-root multi-stage Dockerfiles, and a README that explains architecture better than most open-source projects. But the operational surface contradicts it: the documented quickstart crashes for anyone who isn't the author, the repo publishes a map of a private homelab, version numbers disagree with each other, and CI scans don't look at history. It reads as a strong engineer who iterates fast on a personal production system and hasn't yet done the "stranger's first 10 minutes" pass — exactly the pass that decides interviews.

---
Full evidence trail: `docs/audits/job-ready-progress.md` (now in the schema the updated skill defines — `continue` resumes at Stage 4).
